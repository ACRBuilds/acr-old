name: Jenkins DotOS
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: trigger single Job
      uses: appleboy/jenkins-action@master
      with:
        url: "http://cubegaming.eu:4123"
        user: "AyokaACR"
        token: ${{ secrets.TOKEN }}
        job: "dotos-stylus"
